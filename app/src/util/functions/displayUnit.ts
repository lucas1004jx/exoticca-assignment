export const displayPrice = (unit: number | string) => `$${unit}`
export const displayPercentage = (unit: number | string) => `${unit}%`
export const displayDays = (unit: number | string) => `${unit} days`
